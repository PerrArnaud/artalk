<form method="post" action="{{ path('app_post_crud_delete', {'id': m_o_t_w.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ m_o_t_w.id) }}">
    <button class="btn">Delete</button>
</form>

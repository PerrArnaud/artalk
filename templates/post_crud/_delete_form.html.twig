<form method="post" action="{{ path('app_post_crud_delete', {'id': m_o_t_w.id}) }}" onsubmit="return confirm('Supprimer définitivement cette œuvre ? Cette action est irréversible.');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ m_o_t_w.id) }}">
    <button type="submit" class="btn-delete-large">Supprimer</button>
</form>
